import { BindQueryparamToInputDirective } from './bind-queryparam-to-input.directive';

describe('BindQueryparamToInputDirective', () => {
  it('should create an instance', () => {
    const directive = new BindQueryparamToInputDirective();
    expect(directive).toBeTruthy();
  });
});
