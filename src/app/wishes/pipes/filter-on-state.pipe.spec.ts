import { FilterOnStatePipe } from './filter-on-state.pipe';

describe('FilterOnStatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOnStatePipe();
    expect(pipe).toBeTruthy();
  });
});
